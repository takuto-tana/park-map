import MapSection from "../molecules/MapSection";
const MapArea = () => {
    return(
        <section id="map" className="mb-10">
            <MapSection />
        </section>
    );
};

export default MapArea;